import { ConvertFileToBase64Directive } from './convert-file-to-base64.directive';

describe('ConvertFileToBase64Directive', () => {
  it('should create an instance', () => {
    const directive = new ConvertFileToBase64Directive();
    expect(directive).toBeTruthy();
  });
});
