import { OnlyHebrewLettersInputDirective } from './only-hebrew-letters-input.directive';

describe('OnlyHebrewLettersInputDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyHebrewLettersInputDirective();
    expect(directive).toBeTruthy();
  });
});
